# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build # CORRECTED: Only one dash, correct indentation
      - name: List contents of public directory for debug
        run: | # CORRECTED: 'run' is now indented correctly
          echo "Listing contents of public directory:"
          ls -la public || echo "Public directory does not exist or is empty."
          echo "Listing contents of repository root:"
          ls -la .
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SENALI_235FB }}
          channelId: live
          projectId: senali-235fb
